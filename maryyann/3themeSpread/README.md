# 3themeSpread

Mary Yann–scoped **non-UI** helpers for the Moonflower experiment at `/3-theme-spread`.

- User-facing strings live in `src/lib/i18n/en.ts` under `en.experiments.threeThemeSpread`.
- Svelte UI lives in `src/routes/3-theme-spread/`.
- This folder holds TypeScript types and pure functions only (no React, no Gemini, no API keys).
